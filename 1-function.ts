// ОБЫЧНАЯ
function add(a, b) {
	return a + b
}

// ТИПИЗИРОВАННАЯ
/**
 * в этой ф-ии мы ждем числа !important
 * и возвращаем число !important
 */
function addType(x: number, y: number): number {
	return x + y
}
